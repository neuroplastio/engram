module github.com/neuroplastio/engram

// The minimum a program that imports this needs. engram's own releases are
// built with the toolchain below, which is what makes them reproducible.
go 1.24

toolchain go1.26.5
